---
approved: false
author_words_required: true
override_published: false   # set true only to change frozen published canon
---

# Retcon plan — <book-ordinal> (<book-title>)

Generated by `library retcon-plan`. Set `approved: true` only after the
author has reviewed every row and filled in `new_by_book` and
`author_words` for each.

**Do not apply until `approved: true` and every row has non-empty
`author_words`.** `library retcon --apply` refuses otherwise (exit 2).

## Rows

- entity: <series_id>
  field: <field>
  old_by_book: <current by-book value at this ordinal>
  new_by_book: <author fills — the proposed new by-book map; existing
    by-book entries the row omits are preserved, so a partial map only
    adds or changes the ordinals it names>
  coordinate: <book-N/chapter-MM; required for kind: established-before
    rows>
  author_words: "<author fills verbatim — the reason for the change,
    recorded to the retcon log and never edited>"

Timeline rows use a `kind: established-before` line instead of
`new_by_book`, with `when` (ISO-8601), `summary`, and a mandatory
`coordinate`.

## Author's notes

Free-text. Record the author's reasoning for the plan as a whole, any
rows that touch published canon (these require explicit override and are
marked `kind: errata`), and any judgment-flagged rows the author has
elected to resolve rather than dismiss.

## Status

- `approved: false` — draft, not yet reviewed.
- `approved: true` — author has reviewed and filled all rows; safe to
  `library retcon --apply`.
